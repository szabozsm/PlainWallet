<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlainWallet.Views.LogoSelectionPage"
             Title="Select Logo">

    <VerticalStackLayout Padding="16" Spacing="12">
        <Label Text="Choose a logo" FontAttributes="Bold" />

            <Entry x:Name="FilterEntry" Placeholder="Filter" TextChanged="OnFilterTextChanged" />

       <CollectionView x:Name="LogosCollection"
                SelectionMode="Single"
                SelectionChanged="OnSelectionChanged">
    <CollectionView.ItemTemplate>
        <DataTemplate>
            <Image Source="{Binding .}"
                   Aspect="AspectFill"
                   HeightRequest="100"
                   WidthRequest="100" />
        </DataTemplate>
    </CollectionView.ItemTemplate>
</CollectionView>

        <Button Text="Browse for image" Clicked="OnBrowseClicked" />
        <Button Text="Cancel" Clicked="OnCancelClicked" BackgroundColor="LightGray" />
    </VerticalStackLayout>

</ContentPage>
