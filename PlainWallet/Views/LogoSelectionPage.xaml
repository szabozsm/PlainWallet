<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlainWallet.Views.LogoSelectionPage"
             xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
             xmlns:views="clr-namespace:PlainWallet.Views"
             Title="Select Logo">

    <material:TabView x:Name="tabView"
                      TabPlacement="Bottom">

        <material:TabItem Title="">
            <material:TabItem.HeaderTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}"
                            Command="{Binding Command}"
                            BackgroundColor="Transparent">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="True">
                                <Setter Property="BackgroundColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="TextColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="ImageSource"
                                        Value="list.svg"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="False">
                                <Setter Property="BackgroundColor"
                                        Value="Transparent"/>
                                <Setter Property="TextColor"
                                        Value="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"/>
                                <Setter Property="ImageSource"
                                        Value="list.svg"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                </DataTemplate>
            </material:TabItem.HeaderTemplate>
            <material:TabItem.Content>

                <Grid Padding="16"
                      RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>

                    </Grid.RowDefinitions>

                    <Label Grid.Row="0"
                           Text="Choose a logo"
                           FontAttributes="Bold"/>

                    <Entry Grid.Row="1"
                           x:Name="FilterEntry"
                           Placeholder="Filter by filename"
                           TextChanged="OnFilterTextChanged"/>

                    <CollectionView Grid.Row="2"
                                    x:Name="LogosCollection"
                                    ItemsSource="{Binding Logos}"
                                    SelectionMode="Single"
                                    SelectionChanged="OnSelectionChanged"
                                    ItemsLayout="VerticalGrid, 2"
                                    ItemSizingStrategy="MeasureAllItems">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8"
                                        Margin="0,6"
                                        Stroke="LightGray"
                                        StrokeThickness="1">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="6"/>
                                    </Border.StrokeShape>
                                    <Grid>
                                        <Image Source="{Binding .}"
                                               Aspect="AspectFit"
                                               HeightRequest="140"
                                               HorizontalOptions="Fill"
                                               VerticalOptions="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                </Grid>

            </material:TabItem.Content>
        </material:TabItem>

        <material:TabItem>
            <material:TabItem.HeaderTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}"
                            Command="{Binding Command}"
                            BackgroundColor="Transparent">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="True">
                                <Setter Property="BackgroundColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="TextColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="ImageSource"
                                        Value="web.svg"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="False">
                                <Setter Property="BackgroundColor"
                                        Value="Transparent"/>
                                <Setter Property="TextColor"
                                        Value="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"/>
                                <Setter Property="ImageSource"
                                        Value="web.svg"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                </DataTemplate>
            </material:TabItem.HeaderTemplate>
            <material:TabItem.Content>

                <Grid Padding="16"
                      RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>

                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0"
                          RowDefinitions="*,Auto"
                          ColumnSpacing="8">
                        <Entry Grid.Row="0"
                               x:Name="UrlEntry"
                               Text="{Binding CurrentUrl, Mode=TwoWay}"
                               Placeholder="Enter image URL"
                               HorizontalOptions="Fill"
                               TextChanged="OnUrlTextChanged"/>
                        <Button Grid.Row="1"
                                Text="Use URL"
                                Clicked="OnUseUrlClicked"/>
                    </Grid>

                    <Border Grid.Row="1"
                            Padding="6"
                            Margin="0,6"
                            Stroke="Transparent"
                            BackgroundColor="Transparent">
                        <Image x:Name="UrlPreview"
                               Source="{Binding UrlPreviewSource}"
                               Aspect="AspectFit"
                               HeightRequest="140"
                               HorizontalOptions="Fill"/>
                    </Border>

                </Grid>

            </material:TabItem.Content>
        </material:TabItem>

        <material:TabItem>
            <material:TabItem.HeaderTemplate>
                <DataTemplate>
                    <Button Text="{Binding Title}"
                            Command="{Binding Command}"
                            BackgroundColor="Transparent">
                        <Button.Triggers>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="True">
                                <Setter Property="BackgroundColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="TextColor"
                                        Value="{StaticResource Primary}"/>
                                <Setter Property="ImageSource"
                                        Value="folder_open.svg"/>
                            </DataTrigger>
                            <DataTrigger TargetType="Button"
                                    Binding="{Binding IsSelected}"
                                    Value="False">
                                <Setter Property="BackgroundColor"
                                        Value="Transparent"/>
                                <Setter Property="TextColor"
                                        Value="{AppThemeBinding Light={StaticResource OnSurface}, Dark={StaticResource OnSurfaceDark}}"/>
                                <Setter Property="ImageSource"
                                        Value="folder_open.svg"/>
                            </DataTrigger>
                        </Button.Triggers>
                    </Button>
                </DataTemplate>
            </material:TabItem.HeaderTemplate>
            <material:TabItem.Content>

                <Grid Padding="16"
                      RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>

                    </Grid.RowDefinitions>

                    <Button Grid.Row="0"
                            Text="Browse for image"
                            Clicked="OnBrowseClicked"/>

                    <Border Grid.Row="1"
                            Padding="6"
                            Margin="0,6"
                            Stroke="Transparent"
                            BackgroundColor="Transparent">
                        <Image x:Name="FilePreview"
                               Source="{Binding FilePreviewSource}"
                               Aspect="AspectFit"
                               HeightRequest="140"
                               HorizontalOptions="Fill"/>
                    </Border>

                    <HorizontalStackLayout Grid.Row="0"
                                           Spacing="12">

                    </HorizontalStackLayout>

                </Grid>

            </material:TabItem.Content>
        </material:TabItem>
    </material:TabView>

</ContentPage>
